{% block title %}<h1>Database entry:</h1>{% endblock %}

{% block body %}
    <p>{{ product.id }}</p>
    <p>{{ product.name }}</p>
    <p>{{ product.price }}</p>
    <p>{{ product.description }}</p>
{% endblock %}